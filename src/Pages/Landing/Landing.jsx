import React from 'react';
import './Landing.css';

const Landing = (props) => {
    return (
        <div>
            <p>This is the landing page</p>
        </div>
    );
};

export default Landing;